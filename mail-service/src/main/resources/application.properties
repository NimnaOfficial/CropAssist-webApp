# ===============================================================
# MAIL SERVICE - Application Configuration
# This microservice handles all email sending for CropAssist.
# ===============================================================

# --- Server Configuration ---
server.port=8084
server.servlet.context-path=/cropmgr_app
spring.application.name=mail-service

# --- Email Configuration (Gmail SMTP) ---
# These settings let Spring send emails through Gmail.
# IMPORTANT: Replace the username and password below with your real Gmail and App Password.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nimnakgls980@gmail.com
spring.mail.password=hvospmrjonqresot
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# --- Actuator (Health & Monitoring Endpoints) ---
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
